package com.techelevator;

//INTERFACE Purchasable.java
public interface Purchasable {

    double getPrice();
// Notice that the getPrice() method has no body.
// Interfaces define the method signatures, but don't include the implementation (body) of the method.
// That's the job of the class which implements the interface.



}
